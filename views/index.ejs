<!DOCTYPE html>
<html lang="si">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Documentation - Secure API Base</title>
    <style>
        /* ... (CSS Styles) ... */
        .coin-balance { background-color: #f0f0ff; border: 1px solid #aaa; padding: 10px; border-radius: 4px; margin-bottom: 15px; font-weight: bold; color: #0056b3;}
        .referral-info { background-color: #e6ffed; border-left: 5px solid #4CAF50; padding: 10px; margin-top: 15px; color: #388E3C; border-radius: 4px; }
    </style>
</head>
<body>

    <header>
        <h1>📄 API Documentation</h1>
        <p>Tharuzz-ofc Secure API Base</p>
    </header>

    <div class="container">

        <div class="nav-links">
            <a href="/login">🔑 Login</a>
            <a href="/signup">📝 Sign Up</a>
        </div>
        
        <div class="card">
            <h2>API Endpoints</h2>
            
            <div class="coin-balance">
                Current Coin Balance: <span><%= userCoins %></span> 🪙
            </div>
            
            <% if (user && user.referralCode) { %>
            <div class="referral-info">
                **ඔබේ Referral Code එක:** <code><%= user.referralCode %></code>.
                <br>
                **යොමු කිරීමේ සබැඳිය:** <a href="/ref/<%= user.referralCode %>">
                    <code>/ref/<%= user.referralCode %></code>
                </a>
                <br>
                නව පරිශීලකයෙක් ලියාපදිංචි වන සෑම විටම Coin 50 ක් දිනා ගන්න!
            </div>
            <% } %>

            <div class="note">
                **වැදගත්:** ඔබේ වත්මන් API Key එක: <code><%= userApiKey %></code>.
            </div>
            
            </div>
    </div>

</body>
</html>
